package forth_lab;

public class empty_boxes {//класс пустых клеток
	protected int x;
	protected int y;
	
	public empty_boxes(int x, int y) {//конструктор
		this.x=x;
		this.y=y;
	}
	
	protected int q() {//метод, возвращающий 0, для подсчета количества соседей
		return 0;
	}
	
	protected int k(empty_boxes[][]field) {//метод, в котором мы подсчитываем количество соседей
		int s=0;
		for (int i=-1; i<2; i++) 
			for (int j=-1;j<2; j++ ) 
				s+=field[x+i][y+j].q();
		s-=field[x][y].q();
		return s;
	}
	public empty_boxes nextFill(field field) {//метод, который определяет будет ли закрашена клетка
		empty_boxes[][] nowfield=field.getnowfield();
		int w=this.k(nowfield);
		if (w==3) 
			return new fill_boxes(x,y);
		return this;
	
	}
	public void print() {//вывод клетки
		System.out.print("- ");
	}

}

