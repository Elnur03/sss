/*
Колода карт состоит из 36 листов. Колода перемешивается,
а затем раздается полностью двум игрокам: 1-ая карта – 1-ому игроку,
2-ая карта – 2-ому игроку, 3-ая карта – 1-ому игроку, 4-ая карта – 2-ому игроку и т. д.
Напишите программу, используя объектно-ориентированную методологию,
которая создает объект колода, выводит колоду на экран, перемешивает
колоду, вновь выводит ее на экран. Далее создает объекты игроков,
раздает карты колоды игрокам. При раздаче карты добавляются в конец последовательности,
для этого выполняется проход от начала до конца последовательности и только затем
добавляется очередная карта. Раздав все карты, программа выводит на экран
последовательности карт каждого игрока. Затем выполняются три раунда игры, во время каждого
раунда игроки кладут по одной карте (первой в их последовательности) на стол
(объект последовательность карт). Если достоинство карты 1-го игрока оказывается больше
достоинства карты 2-го игрока, то вся последовательность карт стола помещается в конец
последовательности карт 1-го игрока. Если же достоинство карты 2-го игрока оказывается
больше или равно достоинству карты 1-го игрока, то вся последовательность карт стола
помещается в конец последовательности карт 2-го игрока. После добавления последовательности
карт стола в конец последовательности карт игрока, она становится пустой. Во время каждого
раунда необходимо выводить на экран исходное состояние последовательностей карт игроков,
последовательность карт стола, и состояние последовательностей карт игроков после присоединения
последовательности карт стола. Используйте для хранения колоды и последовательностей
карт игроков и стола только один массив из 36 элементов.
 */

public class Main {

    public static void main(String[] args) {
        Deck deck = new Deck();
        System.out.println("Исходная колода");
        deck.show();
        deck.shuffle();
        System.out.println("После перемешивания");
        deck.show();

        Player.setDeck(deck); //Теперь deck это поле cards

        Player player1 = new Player("Player 1"); //объект первого игрока
        Player player2 = new Player("Player 2"); //объект второго игрока

        deck.distributeCards(player1, player2); // раздача карт

        player1.showCards();
        player2.showCards();

        Player table = new Player("Table"); //Создаём новый объект класса Player "стол"

        for (int i = 0; i < 3; i++) { //С каждым раундом объект стол будет меняться
            System.out.println("Раунд № " + (i + 1));
            Card card1 = player1.takeCard();
            Card card2 = player2.takeCard();
            table.addCard(card1); // добавление карт игроков на стол
            table.addCard(card2);


            table.showCards();

            if (card1.getVal() > card2.getVal()) { //Сравнение номиналов карт
                player1.takeAllCards(table);
            }
            else {
                player2.takeAllCards(table);
            }

            player1.showCards();
            player2.showCards();
        }
    }
}
